{
  "name": "timecraft-mern",
  "version": "1.0.0",
  "description": "TimeCraft MERN Stack with Expo",
  "scripts": {
    "start": "npm run dev",
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "install-all": "npm run install-client && npm run install-server",
    "client": "cd client && npx expo start",
    "server": "cd server && npm run dev",
    "dev": "concurrently --prefix-colors \"cyan,magenta\" --prefix \"[SERVER],[ EXPO ]\" \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}